/** OS/2 + hhea values from the shipped glide-variable.ttf (unitsPerEm 1000). */
export const GLIDE_METRICS = {
  unitsPerEm: 1000,
  capHeight: 709,
  xHeight: 532,
  ascender: 986,
  descender: -277,
} as const;
